# Fidelity v2 candidate review

Status: **approved and implemented** on 2026-07-21

These candidates normalize the two P0 expected designs to the real `1440×1000` acceptance viewport and make the next implementation pass measurable.

## Product truth

- Mission remains durable intent with ordered Waves and executor-specific attempts.
- Agent Team remains an execution attempt with members, correlated assignments, explicit actions, artifacts, pressure, and a parent Wave gate.
- Gate readiness is shown as aggregate `2 / 3`; no missing criterion evidence is invented.
- Provider thinking is neither stored nor rendered as historical evidence.

## Intended visual change

- Keep a continuous workbench rather than a stack of generic cards.
- Give portraits, semantic icons, status color, and attached pressure actions distinct jobs.
- Make the Mission page the overview and the Agent Team page the close-up execution room while sharing one shell and one visual language.
- Use exact viewport geometry as the implementation contract.

The user approved both candidates. Three screenshot stages were retained where needed, exact-size Overlays were reviewed, responsive captures passed without horizontal overflow or console errors, and `visual-contract.json` now records separate passing product-truth and visual-fidelity gates.
