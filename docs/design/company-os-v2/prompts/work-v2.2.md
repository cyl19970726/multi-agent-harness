# Work V2.2 prompt

Base: `v2.2-shared.md`.

Input image role: existing Work V2.0 is the **edit target**. Preserve tabs,
filters, Milestone route, grouped WorkItem ledger, responsibility data, Git
references, and right Milestone context.

Correct the acceptance criteria: financial commitment approval is still
pending and therefore must not appear achieved.

Add:

- an elegant route-marker visual language for Milestones;
- one consistent icon family for WorkItem types;
- canonical assignee portraits or simplified avatar crops;
- subtle row selection, blocker, review, and waiting material treatment;
- a small route/map motif in the Milestone header only.

The ledger remains the center. No charts, no card grid, no Project object, and
no decorative art inside data rows.
