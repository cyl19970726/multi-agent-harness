# Standing Agent Focus · Document Architecture Agent available

Before generating, read and obey `FIXTURE-RULES.md`,
`../fixture-contract.md`, and `page_slices.standing-agent-focus` in the fixed
fixture. They are the only authority for visible business facts.

```text
Create “Standing Agent Focus” using the focus-shell grammar: left navigation, broad central activity/conversation surface, fixed composer, and right context rail. Use the shared visual shell. Select Organization and show the complete navigation.

Center header: “Document Architecture Agent”, “Standing Agent”, and explicit “Available”. The availability label is allowed only because the fixture contains an explicit status report. The central durable entry is exactly “Proposed Create Trademark Management module”, linked to “Trademark Management · Proposed · Pending final approval”. Fixed composer: “Message Document Architecture Agent…”.

Right rail: “Agent profile”, “Explicit status”, and “Governance work”. Show “Availability · Available · explicitly reported” and the one governance proposal. Keep other context sections empty or omit them.

Only the explicit availability report and the described proposal relationship may appear as agent facts. Show no agent telemetry, inferred capacity, assignment, ownership, extra activity, artifact, or visible date.
```
