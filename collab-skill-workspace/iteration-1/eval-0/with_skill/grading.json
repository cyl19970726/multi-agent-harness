{
  "expectations": [
    {
      "text": "work-create includes --team-run-id, --title and --completion-criteria",
      "passed": true,
      "evidence": "Step 2 command: `harness team-run work create --team-run-id tr-123 --title \"Add --json output flag to harness export, with test\" --context ... --completion-criteria \"harness export --json emits one well-formed JSON document ...\" --claim-mode host_assign --owner-member-run-id mr-k1 --idempotency-key ...` includes all three required flags."
    },
    {
      "text": "assigned work uses --claim-mode host_assign together with --owner-member-run-id",
      "passed": true,
      "evidence": "Same work-create command in Step 2 pairs `--claim-mode host_assign` with `--owner-member-run-id mr-k1`."
    },
    {
      "text": "completion criteria name verifiable evidence (PR URL / named check / artifact)",
      "passed": true,
      "evidence": "Completion-criteria text: '...a new automated test exercises --json and asserts the output parses as JSON...; the named test command exits 0; PR opened with a diff scoped to the boundary above; CI green.' Names a PR, a named test command, and CI status as evidence."
    },
    {
      "text": "waiting uses board-summary or cursors/events, never a sleep-loop poll",
      "passed": true,
      "evidence": "Step 3: uses `board-summary`, `work list --brief`, then `work list --since <cursor>`, and states explicitly 'I do not re-issue board-summary in a loop — it's a status snapshot to read at my own safe boundaries ... not something to spin on.'"
    },
    {
      "text": "acceptance happens only after inspecting artifact/check refs; provider completion alone is not acceptance",
      "passed": true,
      "evidence": "Step 8: 'I do not accept on the submission's own \"done\" language, the delivery receipt, or a green check-ref string alone. I open the evidence and walk the completion criteria line by line' — then opens the PR diff, reruns the named test itself, runs `harness export --json` by hand, diffs default output, and confirms CI green before accepting in step 9a."
    },
    {
      "text": "request-changes keeps the same member run/session instead of spawning a replacement",
      "passed": true,
      "evidence": "Step 9b: 'I do **not** spawn a new member or a fresh session for the fix — kiwi continues in the same mr-k1 MemberRun, same worktree, same native session, so it keeps all the context from steps 4–7.'"
    }
  ]
}
