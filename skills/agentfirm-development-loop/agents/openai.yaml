interface:
  display_name: "AgentFirm Development Loop"
  short_description: "Run a simple Task and Review retry loop"
  default_prompt: "Use $agentfirm-development-loop to let the Brain assign a Task, have Dev submit it for exact-version Review, and either finish it or return the same Task for more work."
