# Lead Agent V2.2 prompt

Base: `v2.2-shared.md`.

Input image role: existing Lead Agent V2.1 is the **edit target**. Preserve the
global sidebar, direct-report list, central durable collaboration/activity
stream with composer, and right attention/authority rail.

Add:

- a canonical Company Lead portrait and an understated coordination-field
  motif in the header only;
- expressive, consistent portraits and role-colour markers for direct reports;
- richer artifact tiles for Milestone, WorkItem, Mission summary, and proposal;
- a subtle live-presence pulse near the composer, clearly separate from
  durable Activity;
- slightly more tactile composer and selected-report treatment.

Do not show raw provider transcript or stored thinking. The central stream must
remain the dominant work surface.
