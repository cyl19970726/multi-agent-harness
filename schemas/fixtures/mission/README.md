# Mission fixtures

`valid/basic.json` is the current Mission shape and deliberately omits
`wave_ids`. `valid/legacy-wave-ids.json` proves that ADR 0051-era rows remain
readable for historical export. That compatibility fixture is not a current
write example.
