interface:
  display_name: "Company Org Operator"
  short_description: "Operate Company OS actors, org units, roles, permissions, and page-linked authority refs"
  default_prompt: "Use $company-org-operator to inspect or mutate Company OS Organization through governed Store/API/Action paths while preserving Docs page contracts, Work, Finance, and execution boundaries."
